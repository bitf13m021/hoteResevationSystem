@model List<Hotel_Reservation_System.Models.User_booking>
@{
    ViewBag.Title = "Data";
}



<script src="~/Scripts/jquery.js"></script>
<script type="text/javascript">
    $(function () {


        $(".btn_delete").click(function () {

            //Save the link in a variable called element
            var element = $(this);
            var id=$('#Id')
            //Find the id of the link that was clicked
            var del_id = element.attr("Id");

            //Built a url to send
            var Id = 'id=' + del_id;
                $.ajax({
                    type: "GET",
                    url: "@Url.Action("deleteBooking", "Home")",
                    data: id,
                    success: function () {
                        alert("Data deleted successfully");

                    }
                });
            $(this).parents(".record").animate({ backgroundColor: "#fbc7c7" }, "fast")
           .animate({ opacity: "hide" }, "slow");



        });
        //return false;

    });
</script>

            
                        <table class="table table-striped table-advance table-hover">
                            <tbody>
                                <tr>
                                    <th>User Name</th>
                                    <th>Email</th>
                                    <th>Room Name</th>
                                    <th>Total Room</th>
                                    <th>CheckInDate</th>
                                    <th>CheckOutDate</th>
                                    <th>Price</th>
                                    <th>Action</th>        </tr>

                                @foreach (var data in Model)
                                {
                                    <tr class="record">
                                        <td>@data.user_Name</td>
                                        <td>@data.Email</td>
                                        <td>@data.romm_name</td>
                                        <td>@data.total_room</td>
                                        <td>@data.Idate</td>
                                        <td>@data.odate</td>
                                        <td>@data.TotalPrice</td>
                                        <input type="hidden" value="@data.Id" id="Id" name="Id" />
                                        <td>
                                                   <input type="button" class="btn btn-danger btn_delete" value="delete" id="btndel">
                                           
                                        </td>
                                      
                                    </tr>
                                }
                
                </tbody>
                </table>
    
<!-- container section end -->
<!-- javascripts -->
<script src="~/Scripts/Admin/jquery.js"></script>
<script src="~/Scripts/Admin/bootstrap.min.js"></script>
<!-- nicescroll -->
<script src="~/Scripts/Admin/jquery.scrollTo.min.js"></script>
<script src="~/Scripts/Admin/jquery.nicescroll.js" type="text/javascript"></script>
